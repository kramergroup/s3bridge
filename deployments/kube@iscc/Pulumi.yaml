name: s3bridge
description: The HTML2S3 bridge for video assets etc.
runtime:
  name: nodejs
  options:
    packagemanager: npm
config:
  pulumi:tags:
    value:
      pulumi:template: kubernetes-typescript